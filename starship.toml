# format = '$directory$kubernetes$all$character'
# format = '$directory$all$character'

[time]
disabled = false
format = '🕙[\[$time\]]($style) '
time_format = "%T"

# [kubernetes]
# format = 'on [⛵ $context \($namespace\)](green) '
# disabled = false
# contexts = [
#   { context_pattern = "homelab", symbol = "🏠️ " },
# ]
